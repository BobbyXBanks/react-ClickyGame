//  a component to show the name of the game and instructions

import React from "react";
import "./Title.css";

const Title = props => (
    <div>
        <h1>Clicky Game!</h1>
        <p>Click a different image each time to earn points!</p>
    </div>
);

export default Title;